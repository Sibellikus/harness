interface:
  display_name: "Ship"
  short_description: "Run repository checks before pushing."
  default_prompt: "Use $ship to verify the current branch, run the repository gate, push safely, and create or reuse a pull request or merge request."
